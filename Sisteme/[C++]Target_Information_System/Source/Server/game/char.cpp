// Search for:
#include "DragonSoul.h"

// Add (Under):
#ifdef __SEND_TARGET_INFO__
#include <algorithm>
#include <iterator>
using namespace std;
#endif

// Search for:
	m_dwKillerPID = 0;

// Add (Under):
#ifdef __SEND_TARGET_INFO__
	dwLastTargetInfoPulse = 0;
#endif